services:
  postgres:
    image: postgres:17-alpine
    container_name: har_salon_postgres
    restart: always
    environment:
      POSTGRES_USER: biqydu
      POSTGRES_PASSWORD: biQ1402!
      POSTGRES_DB: hair_salon_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
